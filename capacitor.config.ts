import type { CapacitorConfig } from "@capacitor/cli";
const config: CapacitorConfig = { appId: "com.lumahub.app", appName: "Luma Hub", webDir: "out", server: { androidScheme: "https" } };
export default config;
