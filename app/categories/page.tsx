import { PageHero } from "@/components/features/page-hero"; import { categories } from "@/lib/data/catalog"; export default function Categories(){return <><PageHero eyebrow="Browse" title="A little structure for big possibility." copy="Start with the kind of help or object you are looking for, then follow your curiosity."/><section className="mx-auto grid max-w-7xl gap-5 px-5 pb-20 sm:grid-cols-2">{categories.map(c=><article id={c.id} className={`${c.color} rounded-4xl p-8`} key={c.id}><span className="text-4xl">{c.icon}</span><h2 className="mt-12 font-serif text-3xl">{c.name}</h2><p className="mt-3 max-w-sm leading-7 text-ink/70">{c.description} Explore a carefully growing collection of independent offerings.</p></article>)}</section></>}
