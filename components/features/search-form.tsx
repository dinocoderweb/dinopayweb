"use client";
import { useRouter } from "next/navigation"; import { FormEvent, useState } from "react"; import { SearchIcon } from "@/components/ui/icons";
export function SearchForm({compact=false}:{compact?:boolean}) { const router=useRouter(); const [query,setQuery]=useState(""); function search(event:FormEvent){event.preventDefault(); router.push(`/search?q=${encodeURIComponent(query)}`);} return <form onSubmit={search} className={`flex items-center rounded-2xl bg-white p-1.5 shadow-soft ${compact?"border border-ink/10 shadow-none":""}`}><SearchIcon className="ml-3 size-5 text-ink/45"/><label className="sr-only" htmlFor="site-search">Search Luma Hub</label><input id="site-search" value={query} onChange={(e)=>setQuery(e.target.value)} placeholder="Search products and services" className="min-w-0 flex-1 bg-transparent px-3 py-2 text-sm outline-none placeholder:text-ink/45"/><button className="rounded-xl bg-ink px-4 py-2.5 text-sm font-semibold text-white" type="submit">Search</button></form>; }
