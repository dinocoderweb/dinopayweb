import Link from "next/link"; import type { Listing } from "@/types";
export function ListingCard({item}:{item:Listing}) {return <Link href={`/products?item=${item.id}`} className="group block overflow-hidden rounded-3xl bg-white ring-1 ring-ink/5 transition hover:-translate-y-1 hover:shadow-soft"><div className="relative aspect-[1.18] p-5" style={{background:item.image}}>{item.badge&&<span className="rounded-full bg-white/80 px-3 py-1 text-xs font-bold backdrop-blur">{item.badge}</span>}<div className="absolute bottom-4 right-4 size-12 rounded-full bg-white/70 backdrop-blur transition group-hover:scale-110"/></div><div className="p-5"><div className="mb-2 flex justify-between text-xs font-semibold uppercase tracking-widest text-ink/50"><span>{item.kind}</span><span>★ {item.rating}</span></div><h3 className="font-serif text-xl">{item.name}</h3><p className="mt-2 line-clamp-2 text-sm leading-6 text-ink/65">{item.description}</p><p className="mt-4 font-semibold">{item.price}</p></div></Link>}
