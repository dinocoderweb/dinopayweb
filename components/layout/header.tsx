"use client";
import Link from "next/link"; import { useState } from "react"; import { Button } from "@/components/ui/button"; import { MenuIcon } from "@/components/ui/icons";
const links=[['Services','/services'],['Products','/products'],['Categories','/categories'],['About','/about'],['Contact','/contact']];
export function Header(){const [open,setOpen]=useState(false); return <header className="sticky top-0 z-30 border-b border-ink/5 bg-mist/90 backdrop-blur"><div className="mx-auto flex h-16 max-w-7xl items-center justify-between px-5"><Link href="/" className="font-serif text-2xl tracking-tight">luma<span className="text-coral">.</span></Link><nav className="hidden items-center gap-7 md:flex">{links.map(([label,href])=><Link key={href} href={href} className="text-sm font-medium text-ink/70 hover:text-ink">{label}</Link>)}</nav><div className="hidden gap-2 md:flex"><Button href="/login" variant="light">Log in</Button><Button href="/register">Join Luma</Button></div><button onClick={()=>setOpen(!open)} className="rounded-lg p-2 md:hidden" aria-label="Toggle navigation"><MenuIcon className="size-6"/></button></div>{open&&<nav className="border-t border-ink/5 bg-mist px-5 pb-5 md:hidden">{links.map(([label,href])=><Link onClick={()=>setOpen(false)} key={href} href={href} className="block py-3 text-sm font-semibold">{label}</Link>)}<div className="mt-2 flex gap-2"><Button href="/login" variant="light" className="flex-1">Log in</Button><Button href="/register" className="flex-1">Join Luma</Button></div></nav>}</header>}
